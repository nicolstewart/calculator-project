## Calculator Project

For my Day 10 project, I created a simple calculator. 

The calculator performs basic operations:

    Addition
    Subtraction
    Multiplication 
    Division

The user inputs a number, selects an operation and then inputs a second number. 
After the calculation has been performed, the user can decide to carry on with the result into a new calculation, or start new.
